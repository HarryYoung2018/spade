# Agent Instructions

- Preserve SPADE algorithm behavior unless explicitly asked to change it.
- Keep `README.md`, the project page, and citation metadata consistent.
- Do not fabricate results, links, datasets, checkpoints, posters, or slides.
- Keep the GitHub Pages site static under `docs/`.
- Avoid committing large PDFs, datasets, checkpoints, run outputs, or benchmark dumps.
- Keep all SPADE result numbers consistent with the paper.
- Use relative links inside `docs/` for local site assets.
- Run smoke tests after changes.
